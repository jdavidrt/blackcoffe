Copia de seguridad:
- Local y en la nube
- Del 23 en adelante
- 15 de mayo para atras borrar
- Copias por día a las 11 noche
- Que reestablezca a el día seleccionado
Debe haber una tabla para referenciar los cambios hechos con copias de seguridad ejemplo
"El día X se reestableció la cuenta X con el backup del día X"
C:\Users\jdk_l\.claude\plans\help-me-create-a-shiny-pizza.md
You will help me do a BackupUtility for this project
The user wants to be able to go to a certain day and restore X order to its state on that day
This also can be used as a history for orders
The backup should be done 3 weeks to the past, just monday to saturday
Also the backup should not be done on paid orders, i mean, if a order gets paid on wednesday, the backup for that order should be only be available until that date, including that day when it was paid, but on thursday, the bakup should not be done since the order is already paid and no information will be writen on that one
the backup should be processed by the server every day (except for sundays) at 11pm
also there should be a backup story for orders, were user can see if the order was restored from a backup at some point
Also the user should be able to download a .txt file with the backup of an specific day, with that the user can have a local backup too
there should be a new page called "Copias de seguridad" where the entry point is a calendar to select to which days user want backup and then it should load all the orders backed up from that day, then the user will select an order, see its data an then it can click the option to restore the current order to that day bakup
i want to also make this as lightwaight as possible for the db since a lot of products on json will be stored, if you see any possibility to compress the products info or so, please tell me
also is important to mention that this only its neccesary for orders older than may 15 of 2026 , form orders older than that, we will do a database cleanup at some point
any questions?


help me create a plan to implement this:
# Order Backup / Restore Utility for BlackCoffe

## Goal
Build a daily snapshot system for orders so that any order's historical state can be
browsed and restored, and give users a dedicated "Copias de seguridad" page to do this
through the UI, plus a way to download a given day's backup as a .txt file.

## Scope
- Eligibility is **not** based on `createdAt`. Starting the day this feature is
  deployed (~2026-07-07), any order that is currently **open/unpaid** becomes
  eligible for nightly snapshots from that point forward — including orders created
  long before the deploy date, as long as they're still unpaid when the job first
  runs. New orders are naturally eligible immediately (they start unpaid).
- Orders that are **already paid before the deploy date** are excluded entirely —
  there's no captured history to restore them to, so don't attempt any backfill.
- Do not touch anything under `server/sigale/` (see CLAUDE.md guardrail).

## Snapshot job (cron)
- Use the existing `node-cron` dependency already in root `package.json` (currently
  unused by BlackCoffe — only `server/sigale` has its own copy). Add a new job file,
  e.g. `server/jobs/orderBackup.job.js`, wired up from `server/index.js` alongside
  (not inside) the Sigale mounting code.
- Schedule: every day except Sunday at 23:00 Colombia time — cron expression
  `'0 23 * * 1-6'` with `{ timezone: 'America/Bogota' }`.
- Eligibility per run: an order is snapshotted if `paid = 0`, OR `paid = 1` and it
  became paid *today* (capture the final state once, then never snapshot it again —
  paid orders are already immutable per existing Rule #9).
- **Delta-only writes**: before inserting, compare the order's current
  items/deposit/paid state to its most recent snapshot row. If nothing changed,
  skip — don't write a duplicate row. This is the main lever for keeping the table
  small, since most orders sit untouched for days at a time.
- "Restore to day X" is defined as: reconstruct from the most recent snapshot with
  `snapshotDate <= X` for that order (not a literal 1-row-per-day record).
- **Retention/pruning**: as part of the same nightly run (or a second job right
  after it), delete `order_snapshots` rows where `snapshotDate` is older than 3
  weeks ago. This is a real rolling window, not just a UI limit — old rows are
  actually removed from the DB to keep it lightweight. This also naturally caps how
  far back the "Copias de seguridad" calendar can show data.

## Data model (new tables — propose exact DDL, don't guess blindly at existing conventions)
- `order_snapshots`: `id`, `orderId` (FK orders.id), `snapshotDate` (DATE, Colombia
  calendar day), `itemsGz` (BLOB — gzip'd items JSON via Node's built-in `zlib`, no
  new dependency), `deposit`, `paid`, `paidAt`, `createdAt`. Unique on
  `(orderId, snapshotDate)`.
- `order_restores`: `id`, `orderId`, `snapshotId` (FK), `restoredFromDate`,
  `restoredBy` (string, same `localStorage.getItem('user')` pattern used for
  `collectedBy` elsewhere), `restoredAt`. This is the "backup story" — surface it as
  a small history list/badge on the order (e.g. on the invoice or order detail view):
  "Restaurado desde copia del {date} el {restoredAt} por {restoredBy}".

## Restore semantics
- Restoring overwrites `items` (and the derived total), `paid`, and `paidAt` from
  the snapshot. **`deposit` is never touched by a restore, in either direction.**
- This means restoring can flip a currently-paid order back to unpaid (so its
  deposits become editable again) — that's intentional and is the main reason
  `deposit` is excluded from the overwrite: deposits keep their own independent
  audit trail (Rule #8/#9) and must be corrected through the existing
  delete-deposit / create-deposit flows, not silently rewritten by a restore.
- When `paid` transitions 1→0 via restore, clear `paidAt` to `NULL`, matching the
  existing convention used in `deleteDeposit`.
- Because this bypasses the existing "paid orders are immutable" guard (Rule #9),
  implement it as its own dedicated controller action/endpoint — do NOT route it
  through the generic `updateOrder`, so the existing immutability check elsewhere
  is untouched.
- Every restore confirmation modal must show an **"Importante:"** note (Spanish, to
  match the app's UI language), e.g.: *"Importante: esta restauración no modifica
  los abonos (depósitos) de la orden. Si el nuevo estado requiere abonos
  distintos, elimínelos y regístrelos nuevamente desde esta misma orden."* Follow
  the mandatory "Styling inside Ant Design Modals" rule for the confirm button.
- After restore, write a row to `order_restores`.

## New frontend page: "Copias de seguridad"
- Entry point: a calendar (Ant Design) where only Monday–Saturday dates within the
  3-week retention window are selectable (Sundays disabled, since no job runs then;
  dates older than 3 weeks disabled since the data no longer exists).
- Selecting a day loads all in-scope orders' reconstructed state as of that day
  (i.e., latest snapshot `<= selected day` per order).
- Selecting an order from that list shows its snapshot detail (items, quantities,
  total, paid status as of that day — deposit shown as informational only, not
  restorable) and a "Restaurar a este estado" button (guarded by the confirmation
  modal above, including the "Importante" note).
- A "Descargar copia del día" button downloads a `.txt` file for the whole day: a
  human-readable dump of every order backed up that day (client, items, totals,
  payment status) so the user has an offline local copy.
- Add this page to the nav menu, consistent with existing route/button conventions
  in `Navbar.jsx`.

## Non-goals
- No changes to `server/sigale/*`.
- No backfill of snapshots for orders already paid before deploy.
- Restore never edits the `deposits` table or the order's `deposit` column.

Vamos a hacer unas mejoras a la nueva funcionalidad de copia de seguridad
Vamos a cambiar la manera en que se ve la tabla de productos, no es necesario que tengamos una columna con la información de entregado
En cambio, vamos a colocar en ese columna la hora y fecha de cuando se pidió ese producto ya que es fundamental poder visualizar esta información
De la misma manera, en los archivos de texto descargables, vamos a cambiar el entregado por la hora y fecha de cuando se pidió
También vamos a añadir un separador de días a ambas vistas de los productos, para que sea más fácil de lever
Este separador está pensado para que sea solo de visualización, algo sencillo, como una sola fila con el día 
Este separador también lo vamos a agregar a todas las vistas de productos en toda la página, menos en el formulario de nueva orden, porque no tendría mucho sentido, de resto si es necesario crear esta ayuda visual para poder seguirle la cuenta a las ordenes
También vamos a mejorar la barra de búsqueda para que sea de búsqueda amplia, quiero decir, que si se busca "103 paisa" aparezca el resultado en vez de tener que buscar "103" o "paisa" por aparte, ya que el usuario suele buscar como la combinación sin ningun formato real, puede ser impredecible como tambien "paisa 103" o "103 pais" , esto hace que sea más sencillo buscar en la tabla de ordenes de cada día, puedes usar los campos de búsqueda de por ejemplo "cobrarOrdenes" para que veas como fuciona unabusqueda amplia en este proyecto
También vamos a mejorar el modal y la presentación de la orden que se selecciona para ver su información en la copia
Vamos a colocar el mall en la misma fila que el premises y name, pero en gris y entre paréntesis
Además agregar al final en la parte del total cuanto se tiene registrado en abono en la copia, justo arriba del total, en esa misma jerarquía, para que sea sencilo visualiar cuanto se había abonado a la fecha
también agregar deuda a la fecha computando en el frontend el total de la copia y el deposito de la copia, justo en este orde quedaría
- total
- total abonado en la copia
- deuda al día {día de la copia}
También vamos a convertir a la página de copias en una con clave igual, con la misma clave y todo que "Cuentas por cobrar"


# ============================================================
#  Ableton Live - Deep Cache & Residue Cleaner for Windows
#  Clears: decoding cache, crash dumps, logs, Ableton temp files,
#  installer leftovers, and (optionally) general Windows temp.
#  It does NOT touch Preferences, Database, User Library or Projects.
# ============================================================

# --- Toggles (change before running) ------------------------
$RemoveDownloadedInstallers = $true   # $true = delete Ableton installers in Downloads/Desktop
$CleanWindowsTemp           = $true    # $true = sweep %TEMP% (files older than 1 day only)

$ErrorActionPreference = "SilentlyContinue"

# --- Elevation notice (recommended, not mandatory) ----------
$isAdmin = ([Security.Principal.WindowsPrincipal] `
    [Security.Principal.WindowsIdentity]::GetCurrent()
).IsInRole([Security.Principal.WindowsBuiltInRole]::Administrator)
if (-not $isAdmin) {
    Write-Host "Note: not running as Administrator. Most files will still be removed," -ForegroundColor Yellow
    Write-Host "but if some are locked, re-run this inside an elevated PowerShell." -ForegroundColor Yellow
}

# --- Helpers ------------------------------------------------
function Clear-FolderContents {
    # Deletes everything inside a folder but keeps the folder. Returns MB freed.
    param([string]$Path)
    if (-not (Test-Path $Path)) { return 0 }
    $freed = (Get-ChildItem -Path $Path -Recurse -File -Force |
              Measure-Object -Property Length -Sum).Sum
    Get-ChildItem -Path $Path -Force | Remove-Item -Recurse -Force -ErrorAction SilentlyContinue
    if (-not $freed) { return 0 }
    return [math]::Round($freed / 1MB, 2)
}

function Remove-MatchingFiles {
    # Removes files matching a filter under a root. Optional age filter. Returns MB freed.
    param([string]$Root, [string]$Filter = "*", [int]$OlderThanDays = 0)
    if (-not (Test-Path $Root)) { return 0 }
    $cutoff = (Get-Date).AddDays(-$OlderThanDays)
    $items = Get-ChildItem -Path $Root -Filter $Filter -File -Recurse -Force -ErrorAction SilentlyContinue |
             Where-Object { $OlderThanDays -eq 0 -or $_.LastWriteTime -lt $cutoff }
    $freed = ($items | Measure-Object -Property Length -Sum).Sum
    $items | Remove-Item -Force -ErrorAction SilentlyContinue
    if (-not $freed) { return 0 }
    return [math]::Round($freed / 1MB, 2)
}

function Remove-NamedItems {
    # Removes files/folders whose NAME matches a pattern directly under a root. Returns MB freed.
    param([string]$Root, [string]$Pattern)
    if (-not (Test-Path $Root)) { return 0 }
    $freed = 0
    foreach ($it in (Get-ChildItem -Path $Root -Filter $Pattern -Force -ErrorAction SilentlyContinue)) {
        $freed += (Get-ChildItem $it.FullName -Recurse -File -Force -ErrorAction SilentlyContinue |
                   Measure-Object -Property Length -Sum).Sum
        Remove-Item $it.FullName -Recurse -Force -ErrorAction SilentlyContinue
    }
    if (-not $freed) { return 0 }
    return [math]::Round($freed / 1MB, 2)
}

function Show-Freed {
    param([double]$Mb, [string]$What)
    if ($Mb -gt 0) {
        Write-Host ("Cleaned  {0,10:N2} MB  ->  {1}" -f $Mb, $What) -ForegroundColor Green
    }
}

# --- 1. Close Ableton if it is running ----------------------
$live = Get-Process -Name "Ableton Live*" -ErrorAction SilentlyContinue
if ($live) {
    Write-Host "Ableton Live is running. Closing it first..." -ForegroundColor Yellow
    $live | Stop-Process -Force
    Start-Sleep -Seconds 2
}

$totalFreed  = 0
$abletonRoot = Join-Path $env:APPDATA "Ableton"
Write-Host ""
Write-Host "=== Ableton Live deep cleanup ===" -ForegroundColor Cyan

# --- 2. Decoding cache --------------------------------------
foreach ($p in @((Join-Path $env:APPDATA "Ableton\Cache"),
                 (Join-Path $env:LOCALAPPDATA "Ableton\Cache"))) {
    $mb = Clear-FolderContents $p
    Show-Freed $mb $p
    $totalFreed += $mb
}

# --- 3. Crash dumps (one folder per Live version) -----------
Get-ChildItem $abletonRoot -Directory -Filter "Live *" -ErrorAction SilentlyContinue | ForEach-Object {
    $mb = Clear-FolderContents (Join-Path $_.FullName "CrashDumps")
    Show-Freed $mb (Join-Path $_.Name "CrashDumps")
    $totalFreed += $mb
}

# --- 4. Log files (Log*.txt in each version folder) ---------
$logMb = 0
Get-ChildItem $abletonRoot -Directory -Filter "Live *" -ErrorAction SilentlyContinue | ForEach-Object {
    $logMb += Remove-MatchingFiles -Root $_.FullName -Filter "Log*.txt"
}
Show-Freed $logMb "Ableton log files"
$totalFreed += $logMb

# --- 5. Ableton temp files (name starts with 'Ableton') -----
$tempMb = 0
foreach ($root in @($env:TEMP, (Join-Path $env:LOCALAPPDATA "Temp")) | Select-Object -Unique) {
    $tempMb += Remove-NamedItems -Root $root -Pattern "Ableton*"
}
Show-Freed $tempMb "Ableton temporary files"
$totalFreed += $tempMb

# --- 6. Installer leftovers (Downloads + Desktop) -----------
$installerPatterns = @("Ableton*Live*.exe", "Ableton*Live*.zip", "*Ableton*Suite*.exe", "*Ableton*Suite*.zip")
$installLocations  = @((Join-Path $env:USERPROFILE "Downloads"),
                       (Join-Path $env:USERPROFILE "Desktop"))
$installMb = 0
foreach ($loc in $installLocations) {
    if (-not (Test-Path $loc)) { continue }
    foreach ($pat in $installerPatterns) {
        Get-ChildItem -Path $loc -Filter $pat -File -Force -ErrorAction SilentlyContinue | ForEach-Object {
            $sizeMb = [math]::Round($_.Length / 1MB, 2)
            if ($RemoveDownloadedInstallers) {
                Remove-Item $_.FullName -Force -ErrorAction SilentlyContinue
                $installMb += $sizeMb
                Show-Freed $sizeMb $_.Name
            } else {
                Write-Host ("Installer found ({0,8:N2} MB) -> {1}" -f $sizeMb, $_.FullName) -ForegroundColor DarkYellow
            }
        }
    }
}
if ($RemoveDownloadedInstallers) { $totalFreed += $installMb }
else { Write-Host "(Set `$RemoveDownloadedInstallers = `$true to delete the installers listed above.)" -ForegroundColor DarkGray }

# --- 7. Optional: general Windows temp (files > 1 day) ------
if ($CleanWindowsTemp) {
    $winTempMb = Remove-MatchingFiles -Root $env:TEMP -Filter "*" -OlderThanDays 1
    Show-Freed $winTempMb "Windows %TEMP% (older than 1 day)"
    $totalFreed += $winTempMb
}

# --- 8. Summary ---------------------------------------------
Write-Host ""
Write-Host ("Done. Approximately {0:N2} MB freed." -f $totalFreed) -ForegroundColor Cyan
Write-Host ("Finished at {0}" -f (Get-Date -Format "hh:mm:ss tt")) -ForegroundColor DarkGray
Write-Host "Live wil l rebuild the decoding cache automatically the next time you open a set." -ForegroundColor DarkGray