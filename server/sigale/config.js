// Server configuration (standalone local runs; mirrors BlackCoffe's config.js).
// Port 25060 matches the DigitalOcean / Render convention used by the shared server.
export const PORT = process.env.PORT || 25060;
